name: 每日解析在线本地包

on:
  schedule:
    # 每天 UTC 17:00 运行（北京时间次日 01:00）
    - cron: '0 17 * * *'
  # 保留手动触发：在 Actions 页面点一下就能立即跑一次，不受 cron 限制
  workflow_dispatch:

concurrency:
  group: daily-ry-parser
  cancel-in-progress: false

jobs:
  parse:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 安装依赖
        run: pip3 install requests

      - name: 清空旧产物（强制覆盖）
        run: |
          rm -rf ry
          mkdir ry

      - name: 运行解析器
        run: python3 "python/ry下载器.py"

      - name: 提交结果
        run: |
          git config user.name  "ry-parser-bot[bot]"
          git config user.email "ry-parser-bot[bot]@users.noreply.github.com"
          git add ry/
          if git diff --cached --quiet; then
            echo "没有变化，跳过提交"
          else
            git commit -m "chore(ry): 每日更新 ry 解析结果 $(date -u +%Y-%m-%d)"
            for i in 1 2 3; do
              git pull --rebase origin ${{ github.ref_name }} && git push && break
              sleep 5
            done
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
